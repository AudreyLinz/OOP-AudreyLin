rand() library is #include <cstdlib>
    add #include<ctime> for completely random

cplusplus.com is good recourse

git clone https://github.com/johndoe/my-cpp-project.git
cd my-cpp-project

g++ hello.cpp
./a.out

should start code with:
#include<iostream>

using namespace std;

int main(){
    //all code stuff
    return 0;
}

ceil(x) x will round up to the nearest integer
floor(x) x will round down to the nearest integer

max(2,3) returns 3
min(2, 3) returns 2
abs(-2) returns 2